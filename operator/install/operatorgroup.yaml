# AllNamespaces OperatorGroup (empty spec) — the operator watches Dotvirt CRs
# cluster-wide. One group per namespace.
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: dotvirt-operator
  namespace: dotvirt-operator
spec: {}
