// Class strings the hand-rolled tables repeat verbatim. Constants, not a
// DataTable component: each table keeps its own markup and quirks, only the
// exactly-shared chrome lives here.
export const THEAD = 'text-left text-xs tracking-wide text-ink-faint uppercase';
export const THEAD_TR = 'border-b border-line';
export const TH = 'py-1.5 pr-3 font-medium';
export const TH_LAST = 'py-1.5 font-medium';
export const TBODY = 'divide-y divide-line-soft';
